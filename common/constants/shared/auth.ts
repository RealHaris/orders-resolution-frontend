/** Password length limits matching the backend signup validator. */
export const AUTH_PASSWORD_MIN_LENGTH = 8;

/** Maximum password length accepted by the backend. */
export const AUTH_PASSWORD_MAX_LENGTH = 128;

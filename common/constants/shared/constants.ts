/** Default API origin used when `NEXT_PUBLIC_API_HOST` is unset in development. */
export const DEFAULT_API_HOST = "http://localhost:6011/api";

/** HttpOnly session cookie set by the backend. */
export const ACCESS_TOKEN_COOKIE = "accessToken";
